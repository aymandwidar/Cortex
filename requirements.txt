# Cortex V2.5: Cloud-Native - Optimized for Render Free Tier (512MB limit)
# LIPOSUCTION: Removed torch, torchvision, sentence-transformers (300MB+ saved)

# Core FastAPI stack
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
litellm>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
httpx>=0.25.0
structlog>=23.2.0
pyyaml>=6.0

# Database support
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# Memory & Vector Database (KEPT for agentic features)
qdrant-client>=1.7.0
numpy>=1.24.0

# Optional services
redis>=5.0.0
vaderSentiment>=3.3.2
prometheus-client>=0.19.0
cryptography>=41.0.0

# BRAIN TRANSPLANT: Using cloud embeddings via LiteLLM instead of local models
